# Copy to `.env` for local Capacitor / APK web builds.
# The Android APK loads bundled web assets and forwards /api/* here.

VITE_APP_CHANNEL=production
VITE_API_BASE_URL=https://YOUR-WORKER.workers.dev
